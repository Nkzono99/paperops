\section{Method}

% block: method.structure.01
構成は `manuscript/`、`refs/`、`notes/`、`.claude/` の四層を中核とする。

% block: method.mirror.01
各日英 section には `% block: ...` ID を付与し、`manuscript/mirror/map.toml` で対応関係を管理する。

% block: method.automation.01
CI と hooks は、文献整合、mirror 検査、保護対象ファイルの編集抑止を担う。
