\section{Method}

% block: method.structure.01
構成は `story/`、`manuscript/`、`submission/`、`_paperops/` の境界を中核とし、下流の project 固有状態と managed defaults を分ける。

% block: method.mirror.01
各日英 section には `% block: ...` ID を付与し、`manuscript/mirror/map.toml` で対応関係を管理する。

% block: method.automation.01
Make target と検証 script は、文献整合、mirror 検査、カード接続、投稿前 gate を段階別に確認する。
