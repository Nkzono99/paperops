# Local Paths

Copy `locations.example.toml` to `locations.toml` for machine-specific paths.
Keep `locations.toml` untracked.

Use the same alias names as `refs/links.toml` entries reference through
`local_path_alias`. The shared registry belongs in Git; absolute local paths do not.
