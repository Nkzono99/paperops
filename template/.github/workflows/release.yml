name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: Nkzono99/paperops/.github/workflows/reusable-release.yml@main
    with:
      working-directory: .
      artifact-name: arxiv-export
