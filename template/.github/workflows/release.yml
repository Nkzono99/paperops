name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: YOUR_ORG/paperops/.github/workflows/reusable-release.yml@main
    with:
      working-directory: .
      artifact-name: arxiv-export
