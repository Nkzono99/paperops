name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: Nkzono99/paperops/.github/workflows/reusable-build.yml@main
    with:
      working-directory: .
      run-latex: false
